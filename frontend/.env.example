## Пример переменных окружения для Vite
## ВАЖНО: VITE_* переменные «вшиваются» в сборку фронтенда.
## Рекомендуем указывать origin бэкенда (без /api), но код также корректно переварит /api в конце.
VITE_API_BASE=http://localhost:8000
